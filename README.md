# My App Projects

A collection of Flutter mobile application projects.

## Projects

| Project | Description |
|---------|-------------|
| [hello_flutter](./hello_flutter) | A simple "Hello World" Flutter starter app |

## Getting Started

Each project is self-contained inside its own folder. To run any project:

1. Make sure [Flutter](https://flutter.dev/docs/get-started/install) is installed on your machine.
2. Navigate into the project directory:
   ```bash
   cd hello_flutter
   ```
3. Install dependencies:
   ```bash
   flutter pub get
   ```
4. Run the app:
   ```bash
   flutter run
   ```

## Requirements

- [Flutter SDK](https://flutter.dev/docs/get-started/install) (stable channel recommended)
- Android Studio / Xcode for device emulation
- Dart SDK (bundled with Flutter)

## License

This repository is open source. See individual project folders for details.
